# Baseline Evidence - 2026-02-18

## Scope
- Release stabilization quality gates
- Role/permission UX parity checks
- Error contract and supportability checks

## Command Evidence

### Backend
- `composer validate --strict`: PASS
- `php artisan test --filter "(ApiObservabilityTest|RbacAccessTest|SnagTransitionTest|MobileSyncTest)"`: PASS
- `php artisan test`: PASS (51 tests, 400 assertions)

### Web
- `npm run lint`: PASS
- `npm run test -- --run src/layout/AppLayout.test.jsx src/components/PermissionRoute.test.jsx src/utils/apiError.test.js`: PASS
- `npm run test -- --run`: PASS (11 files, 26 tests)
- `npm run build`: PASS
  - Known warning: main bundle chunk remains over 500 kB after minification.

### Mobile
- `npm run typecheck`: PASS

## Script Validation
- `scripts/release-quality-gates.ps1` now enforces external command exit codes.
- Local Windows fallback behavior is enabled for `npm ci` file-lock cases:
  - `npm ci` first
  - fallback to `npm install` only outside CI
- CI behavior remains strict (`npm ci` with no fallback in CI environments).

## Notes
- Local machine showed intermittent Windows `EPERM` unlink locks under `web/node_modules` during `npm ci`.
- Stabilization checks remained green after fallback path and full gate execution.
